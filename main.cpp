#include <iostream>
#include "object.h"


int main()
{
	size_t count = 15;
	Container<Task&> &sample();
	sample.size();
	for (size_t i = 0; i < count;++i)
	{
		auto a = RandomTask(i);
		sample.PushBack(a);
	}
}